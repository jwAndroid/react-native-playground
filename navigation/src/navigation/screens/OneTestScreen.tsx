import React, { memo } from 'react';
import { View, Text } from 'react-native';

function OneTestScreen() {
  return (
    <View>
      <Text>OneTestScreen</Text>
    </View>
  );
}

export default memo(OneTestScreen);
